// Test the authority's actual host-independent replay helpers without importing
// the generated WASM database host functions into a native test executable.
#[path = "../src/round_flow.rs"]
mod round_flow;
