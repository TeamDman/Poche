[package]
name = "poche-bevy-0-19-probe"
version = "0.0.0"
edition = "2024"
rust-version = "1.96"
license = "MPL-2.0"
publish = false

[dependencies]
bevy = { version = "=0.19.0", default-features = false, features = ["3d"] }

# Keep this exact-version compatibility probe outside the product workspace.
[workspace]
