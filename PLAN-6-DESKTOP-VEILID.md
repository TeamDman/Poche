# Desktop Veilid lobby and recovery

**Plan status:** Active; user authorized goal execution
**Primary implementation root:** `D:\Repos\Games\poche-3`, `model-checking`
**Last updated:** 2026-09-09
**Intent audit:** Three passes completed for the September restart; scope gates pending

## Authorized execution contract

User explicitly requested setting the goal and proceeding after the final scope summary. G1 uses automatic protected same-device identity recovery; recovery-code export is outside this goal. G3 uses strict hand privacy in this desktop slice; retain legacy grant experiments without exposing them here. G4 requires two real desktop processes, create/join/seats/deal, synchronized full position/rotation manipulation and cross-viewport dragging, successful legal play and denied out-of-turn play without logical mutation, reveal, or forced snap-back. A complete trick is a validation witness, not a promise to finish all remaining full-game UX. G2's lifetime decision is confirmed; implement and document failure-detection and replication details before claiming recovery. These resolutions supersede the alternative proposals in the historical gate rows below. No further user approval is required for reversible internal choices within this scope.

Execution audit: rechecked U1-U17 extraction against the available September exchange; mapped each to tasks/gates; checked inverse scope for web removal, recovery export, shared versus local motion, physical/logical independence, and privacy. User approval closes product-scope questions; technical gates remain evidence obligations, not requests to reopen the agreed goal.

## How to update this plan

- `[ ]` Not started; `[~]` in progress; `[x]` complete; `[!]` blocked with evidence and unblock condition.
- Update task headings and completion notes together. Keep one implementation focus.
- Record actual commands, outcomes, decisions, commits, and exceptions under their task, not in a detached log.
- A phase is complete only when all its tasks are complete. Do not infer completion from older plans.
- The plan is only ready once we have literally triple checked that no intent from the user has been omitted without explicit direction from the user.

## Intent audit evidence

1. Extraction: inspected the available September research/restart messages through the crash-rejoin suggestion. Captured desktop preference, corrected reference path, Bevy patch request, clipboard qualifier, spatial agency, spectators, recovery, and the tentative nature of name/secret authentication as U1-U12.
2. Traceability: every row below maps to a task or gate. Recommendations (one-trick slice, recovery-code design, Windows-first support) are explicitly not user-approved requirements.
3. Adversarial review: checked that leaving is not permanent, an invitation is not a player credential, multiple devices retain agency, mock nodes do not connect separate processes, and a camera layer does not provide secrecy. Preserved prior work rather than declaring all historic features deleted.
Source boundary: the September user messages are available. Prior requirements remain in the existing plans, especially PLAN-5-LIVE-CONTROL-PUPPETS.md; its ledger was inspected. This is not a new certification of every historic implementation or an exhaustive re-audit of all five plans.

Amendment audit (September 9): extraction added U13 from the explicit everyone-leaves clarification; traceability maps it to G2/T5; adversarial review preserves crash recovery while peers remain, distinguishes intentional departure from temporary loss, and does not infer that cached DHT data resurrects a disbanded room. Rechecked U1-U12 mappings; other gates remain open.

Movement amendment audit: extraction added U14-U17 from the user's correction of the proposed legal-play-only/local-rearrangement scope. Traceability maps these to T4a/T4b and G4. Adversarial pass rechecked all ledger mappings and preserved cross-client (not merely local) motion, rotation as well as position, one shared world across viewports, and the tentative 'perhaps' concerning denied-play physical pose. No implementation/proof is claimed by this audit.

## Purpose and guidance ledger

Create a coherent desktop experience: independently launched games create/join a lobby, occupy seats around a table, and recover player identity after a crash. Preserve the independently executable rules and formal evidence.

| ID | Guidance and qualifier | Coverage |
| --- | --- | --- |
| U1 | Prioritize desktop; dropping web support may be best. | T1/T6: desktop is this slice; preserve existing web sources, do not silently delete or promise maintenance. |
| U2 | Bevy stays; update to 0.19.1. | T1 |
| U3 | Understand transport abstraction and exploit new Veilid local testing. | T2; one real adapter exercised by mock and live core; retain device semantics. |
| U4 | Open two games to a main menu; Create lobby produces a code with Copy. | T3/T6; distinct profiles for two local players. |
| U5 | Join lobby reads clipboard and prefills a text area iff expected format matches; user clicks Join. | T3; no automatic joining or logging arbitrary clipboard content. |
| U6 | Both windows show a table, seats, and seat-taking. | T4/T6; seat conflicts explicitly rejected. |
| U7 | Tabletop-style object actions have permissions; Poche forbids spawning after initial deck and revealing hand cards except by playing. | T4/G3; no unrestricted spawn endpoint; independent validation, not only disabled controls. |
| U8 | Cards grounded in 3D; knowing faces and positioning cards are engine permissions. A second camera may show the hand at the bottom without background clutter. | T4; camera is an optional rendering technique, not secrecy; shared identity, no duplicate canonical cards. |
| U9 | Spectators are spatial participants, e.g. capsules around the room until seated. | T4 |
| U10 | Crash must allow rejoining; perhaps use the same name and secret. | G1/G2/T5; required recovery outcome, tentative credential mechanism. |
| U11 | Research paths: D:\Repos\rust\veilid, D:\Repos\rust\makepad, D:\Repos\Games\bevy, corrected D:\Repos\Games\bevy_veilid, G:\Programming\Repos\ash. bevy_veilid is reference-only, not a dependency. | T1/T2; Bevy selected, alternatives not an implementation obligation. |
| U12 | Use G:\Programming\Repos\skills\.github\skills\resumable-implementation-plans\SKILL.md to stay on track. | This living contract and T6 audit. |
| U13 | If everyone leaves the lobby it gets disbanded. | G2/T5: no requirement to restore an empty room later; credentials do not resurrect a disbanded room. |
| U14 | Physical location is position plus rotation; logical location is hand, deck at index n, or in play. Rules use logical location. | T4a; supersedes agent suggestion to restrict movement to legal play/local-only arrangement. |
| U15 | Wiggling a hand card in one client must be visible in the other client. | T4a/T6; shared authorized poses even when logical state does not change; hidden faces remain hidden. |
| U16 | Dragging hand-to-table crosses camera viewports whose projections map to the same 3D space. | T4b; continuous same-card drag, no canonical duplication or unrelated local coordinate state. |
| U17 | Entering the play area attempts a rules action; if not the player's turn, perhaps logical position stays unchanged even though physical position changed. | T4a/T4b; separate pose acceptance and play acceptance. Proposed MVP preserves allowed pose on denial with explicit status; no implicit reveal. |

Inherited constraints: MPL-2.0; one poche executable with CLI and GUI; Figue/Teamy CLI conventions; no Vox/local-instance control foundation; distinct authorized device keys per player, no extra votes from extra devices; typed observations/actions; same-player capture protocol; windowless puppets; preserve model scopes and independent oracles. Previous leave clarification remains: voluntary leavers may rejoin as spectators and take a seat at an appropriate phase; crash reconnect is distinct from voluntary leave, kick, or revoked credentials.

## Verified foundation, not fresh test evidence

- Workspace pins Bevy `=0.19.0`; lockfile agrees. crates.io API reports stable 0.19.1 on September 9.
- `crates/poche-native-ui/src/native_live.rs`: worker-thread bridge generic over DeviceTransport. CLI `cli/live_device.rs` concretely selects HTTP today.
- `crates/poche-runtime/src/oracle_session.rs`: pure rules adapter; renderer does not own legality or scoring.
- `crates/poche-player-client/src/protected_store.rs`: public profiles reference protected key storage; existing API deliberately has no secret export. Name/password recovery does not already exist merely because profiles do.
- `crates/poche-veilid`: optional native probes, not the default GUI transport.
- Veilid upstream 99c9616295f9195c97bf2f5f6b43f0d220ac5d8b has mock-api and faults; published core is still 0.5.7. Mock shared state is process-local; separate game processes require actual networking.
- PLAN-5 is marked complete historically. Previously reported external full-game PUPPET-NO-ACTION needs fresh reproduction; do not claim it fixed or assign a cause without evidence.
- No build or test was run while creating this document.

## Gates: close before affected implementation

| Gate | Required choice / proposed default | Acceptance consequence |
| --- | --- | --- |
| G1 Recovery UX | Recommend automatic same-device recovery from protected persistent profile; optional generated high-entropy recovery code for lost profile/new device, not a user-chosen password. Name is a label. User's name+secret proposal remains tentative. | Restore same principal without name impersonation; lost-secret behavior, credential scope, export consent, revocation and device enrollment tests. Never silently add export to existing non-exportable keys. |
| G2 Failure contract — lifetime scope settled, protocol details open | U13: everyone leaving disbands the lobby; durable all-peer shutdown/resume is not required. Participant crash recovery remains required while peers retain the room, including creator failure. Proposed policy: explicit final departure closes immediately; unexpected loss uses a bounded reconnect grace period, not instantaneous disbanding on a missed message. Choose timeout, partition-safe membership/expiry rules, seat retention, absence progression, snapshot verification and replay deduplication before implementation. | Killed process rejoins with correct entitled state while room survives; final departure and timeout cases cannot revive a closed room through stale invitations/DHT records. Revoked devices denied; creator failure covered without claiming consensus/failover already solved. |
| G3 Reveal policy | Latest Poche example disallows hand reveal except play; earlier plans supported spectator hand grants. Confirm strict default Poche mode versus removing grants entirely. | No newly enabled reveal bypass; do not silently remove previous policy features or equate render visibility with authorization. |
| G4 Slice endpoint — movement clarified | User requires two instances create/join and move cards, specifically synchronized physical poses distinct from logical rules state (U14-U17). Include ready/deal to exercise hand-to-play valid and out-of-turn attempts; one complete trick remains an agent-proposed acceptance witness, not a full-game promise. | Local-only wiggle or legal-action-only dragging does not pass. End-to-end acceptance covers accepted pose with rejected play as well as a successful play. |

Working support assumption: Windows desktop first (two local independent processes); no web acceptance in this slice, no Makepad/ash migration, no generalized tabletop editor, new RL learner, or new mental-poker protocol. Network transport is not itself decentralized game authority. Preserve existing consensus/security work without claiming it production-ready.

## Tasks and validation

All commands run from the primary root. Commands below are existing package surfaces, not claimed successful results. Add exact new test/launch commands alongside implementation before marking done; never invent a passing future harness invocation.

### [x] T1 Update Bevy and establish baseline

Completion disposition: Bevy 0.19.1 is locked and compiles through the desktop CLI; baseline suites and inspected windowless captures are recorded below. The intermittent old capture rejection remains an explicit T6 risk, not a claimed fix. Baseline establishment is complete; integration focus is T2. Later notes below are historical checkpoints, not current process handles.

Completion notes: goal activated September 9 after explicit user approval. Starting tree contains only this newly authored plan as untracked work. Root Bevy pin updated to 0.19.1; lock resolution and tests pending. User permits updating local Bevy reference checkout, but preserve its untracked assets and do not use a local path dependency.

Dependency resolution: sandbox network attempt failed; approved network retry `cargo update -p bevy --precise 0.19.1` succeeded, updating the Bevy family and adding its clipboard transitive packages. `cargo test --locked -p poche-native-ui -p poche-puppet -p poche-capture` is building (terminal session 80091); not yet a passing result.

Baseline result: session 80091 completed successfully: capture 8, native UI 14, headless puppet 4 passed; six browser/external/GPU tests explicitly ignored. This proves baseline source compatibility, not real Veilid or GPU rendering. Session 19407 `cargo test --locked -p poche-spatial --offline` completed: 31 passed including all three new manipulation tests. Windowless GPU acceptance launched separately; T1 remains in progress pending capture inspection and remaining baseline checks.

Checkpoint commit: 5451022 contains Bevy pin/lock upgrade, this plan and initial pose reducer. GPU result (session 88270): `$env:WGPU_BACKEND='dx12'; cargo test --locked -p poche-puppet --offline --test native_full_game -- --ignored --nocapture` built successfully but failed at `tests/native_full_game.rs:27`: DeviceProtocol, 'certified native capture device rejected the puppet operation'. No screenshot verified. Next T1 action: trace capture-provider rejection and recover precise underlying error rather than misclassify this as a GPU failure. Previous turn changed source and established test evidence (progress), not a blocked state.

Follow-up evidence: native puppet now preserves DeviceClientError class in redacted messages and logs enum-only readback denial stages. Diagnostic regression passed via `cargo test --locked -p poche-puppet --offline --lib capture_diagnostics`. Two reruns of native_full_game passed (69.73s and 79.14s) without a behavioral fix; original rejection remains intermittent/unexplained, not resolved. Added optional `POCHE_NATIVE_EVIDENCE_ROOT` to ignored GPU test for retained captures; second rerun used `D:\Repos\Games\poche-3\target\phase6-native-evidence`. Six captures published under `two-player-full-round-seed-29`. Inspected `captures/native-1d76fdaaeca77442/native-1d76fdaaeca77442-png.png` with view_image: nonblank 3D card-selection table, filled small labels; near avatar occludes hand and score sheet is tiny. T4 must improve these. This remains loopback device evidence, not two-process Veilid acceptance. Next work: real Veilid adapter integration, retain improved diagnostics for recurrence rather than claim this flake fixed.

T2 reconnaissance while baseline compiles: `replicated.rs` requires strict player-majority and joint membership quorums; in a two-player room one survivor cannot finalize new logical actions alone. Preserve safety: recovery may retain the room while waiting for the other player; do not silently lower quorum to claim availability. Creator restart must not depend on the creator retaining the only copy of accepted state. `bin/native_smoke.rs` currently has a HostRuntime with one InProcessAuthority and is not failover evidence. Resolve and test the replica/recovery path before T2 completion.

Work: inspect current dirty state, workflow `.github/workflows/pages.yml`, dependency pins, and native captures. Update root Cargo.toml/lockfile to 0.19.1 without sibling path dependencies. Reproduce historical puppet failure separately.

Validation: `cargo test --locked -p poche-native-ui -p poche-puppet -p poche-capture`; `cargo run --locked -p poche-cli -- --help`. Inspect fresh windowless captures via view_image. Network dependency download may be required; do not force offline before cache exists.

Complete when patch is locked, source builds, baseline results and any failures are recorded with exact commands. No claim of upgrade verification from manifest edit alone.

### [~] T2 Establish Veilid device transport

Two-process real-network milestone: new opt-in `crates/poche-cli/src/cli/desktop/process_probe.rs` spawns two independently owned test processes with CREATE_NO_WINDOW, each using the production connector/protected profiles/NativeLiveDevice. Parent-owned temporary files carry only invitation and completion markers; seat/ready/countdown/deal actions use Veilid, not file IPC. Parent guards kill/reap only its own children on failure and TempDir removes the invitation. Test profile identities remain in protected storage. Command outside sandbox: `$env:POCHE_ALLOW_VEILID_PUBLIC_TEST='I_ACCEPT_PUBLIC_NETWORK_TRAFFIC'; cargo test --locked -p poche-cli --offline protected_desktop_two_process -- --ignored --nocapture`.

Evidence: first run failed on creator initial observation (14.23s); first-read-only retry experiment reached committed Create but failed on confirmation read (29.96s). Moved bounded retry to all observation RPCs, reusing the same encoded signed read; writes remain single-shot, and authorization/protocol reply denials are not retried. Final two-process run passed in 44.18s with two retry messages, profile suffix 1788982549099. Both independently running devices obtained private hands and had no granted-hand projections. Retry unit test verifies success on third read, stop after three unavailable reads, and immediate authorization rejection. This is real process/network integration but NO Bevy rendering/input/clipboard, motion, process-kill recovery or final-disband evidence. Remaining network reliability is not declared universally solved by one passing run.

Native transient-failure handling: the live worker no longer terminates on StaleRevision, NoProgress or TransportUnavailable. It reports the error and resumes reads after a 500 ms delay; it never automatically replays a possibly committed write. Authentication/protocol errors remain terminal. The renderer now uses any valid observation received before an error while keeping that error visible. New native regression races a sibling's accepted play against the native client's stale action, requires the stale error, then verifies the same worker receives a subsequent opponent play. Focused test passed (0.52s). This is loopback worker recovery, not route rediscovery, process restart or creator failover, and no network-fault injection is claimed.

Failed-startup cleanup: the production connector now groups invitation encoding, creator initialization, and initial NativeLiveDevice observation before handing publication ownership to the app. Any error in that group stops the service and explicitly closes the publication while the node remains alive, with only a static cleanup-failure message. `cargo check --locked -p poche-cli --offline` passed. `cargo test --locked -p poche-veilid --features veilid-mock-test --offline --test mock_nodes` passed; its new assertion verifies a closed publication's private route cannot be released a second time. This tests the cleanup primitive, not injected failure of each connector stage. It does not erase cached DHT data, prove final-departure disbanding, or fix the intermittent creator initialization failure.

Production connector probe: extracted the menu closure into the same private `connect` function used by the worker and added ignored `protected_desktop_create_join` in `crates/poche-cli/src/cli/desktop/connection.rs`. Opt-in command (outside sandbox for OS vault/network): `$env:POCHE_ALLOW_VEILID_PUBLIC_TEST='I_ACCEPT_PUBLIC_NETWORK_TRAFFIC'; cargo test --locked -p poche-cli --offline protected_desktop_create_join -- --ignored --nocapture`. It opens no windows, uses fresh uniquely named persistent test profiles, and drives NativeLiveDevice actions while waiting for both clients' revisions. First execution failed after successful attach/publication with generic creator-initialization failure (18.50s). Added redacted stage diagnostics in create_device; rerun passed create/join/seats/ready/countdown/dealt own hands over actual public Veilid in 23.43s, without a behavioral fix. Therefore startup failure is intermittent/unexplained, not fixed. Passing profile suffix: 1788981436691; protected probe identities are retained, not user credentials overwritten. This is two native nodes in ONE process, not two-process, graphical-input, clipboard, capture or creator-failover acceptance. Next: reproduce with stage diagnostics and build external two-process/windowless acceptance around this exact connector.

Automatic countdown/deal integration: published rooms now enroll freshly certified internal clock/environment principals. The owned service runs bounded scheduler ticks (at most four transitions per 50 ms), through the existing advertised-action/reducer path. Reversible desktop countdown default is three seconds from scheduler observation of the arm event; this may lengthen, never shorten, the abort window. A committed arm revision identifies a new countdown even when its token is reused and an abort/rearm occurs between polls. No wall clock is read by the reducer. Internal service identities remain local/ephemeral: this is not creator recovery or distributed scheduling.

Strict lint audit attempted with `cargo clippy --locked -p poche-veilid --features device-service --offline --lib -- -D warnings`; stopped in existing `poche-spatial/src/manipulation.rs` missing Errors documentation on new/update/change_lease. Not a passing lint gate; fix and rerun during T6 before release.

Lint follow-up: added those manipulation error docs. Rerun progressed to reusable invitation constructor documentation (missing backticks/Errors); corrected those too. Full strict lint has not yet passed, and remains a release obligation.

Validation: `cargo test --locked -p poche-runtime --offline --lib` passed all 34 tests, including delayed expiry and abort/rearm generation reset. `cargo test --locked -p poche-veilid --features device-service,veilid-mock-test --offline --test device_service` passed (3.94s): the assembled published room, two certified clients, seat/ready/start actions and automatic expiry plus dealt private hand traverse mock private-route RPCs. `cargo check --locked -p poche-cli --offline` passed with non-mock features. This supersedes the older missing-driver notes below; real protected two-process startup, menu input, replicated recovery, and final-departure teardown remain unproven. Next focus: exercise the actual protected desktop connection path in an external/windowless harness, not a second fixture assembly.

Live-network evidence: approved non-mock `POCHE_ALLOW_VEILID_PUBLIC_TEST=I_ACCEPT_PUBLIC_NETWORK_TRAFFIC; cargo run --locked -p poche-veilid --features veilid-public-test --offline --bin poche-veilid-public-probe` passed: dht=true, private_route=true, app_call=true, host_peers=64/client_peers=64. This is the legacy two-node/single-process probe with temporary insecure test storage, not the protected desktop path or two-process goal acceptance. Node now exposes bounded attach_public that waits for public_internet_ready rather than mistaking attach acknowledgement for usable networking; its mock lifecycle test passed.

Reusable invitation correction: InviteRecord::new remains single-use; explicit new_reusable is selected by publish_device_room so the shared lobby code can admit more than one spectator and permit leaving/rejoining. MemberJoined consumes only single-use records. New session test covers bob/carol admission, bob leave/return, then revocation. Transcript semantic hashing includes reusable=true while omitting false to preserve legacy single-use hashes. `cargo test --locked -p poche-session -p poche-runtime --offline --lib` passed (33 session, 34 runtime); assembled Veilid mock integration passed (0.66s). Formal-model/coverage updates for the new policy and a real-client voluntary-leave/rejoin test remain T4/T5 evidence obligations; do not infer them from the pure session test.

Room assembly checkpoint: `publish_device_room` generates a fresh room ID/seed, publishes its encrypted rendezvous, installs the derived invite verifier in a two-seat pending session, and starts the owned bounded service. Returns both publication and service owners; callers must retain them and initialize the creator before exposing the code. The integration uses this assembly plus create_device/join_device and passes (`cargo test --locked -p poche-veilid --features device-service,veilid-mock-test --offline --test device_service`, 0.66s); non-mock service check passed. Reversible lifetime default: invitations last for the lobby, with u64::MAX code/revision deadlines, matching rejoin-with-code intent. This supersedes a requirement to invent a short wall-clock expiry for this MVP; disband/revocation must close the session and withdraw the route. Finite-expiry support would require server-side deadline enforcement before being advertised. This remains central two-seat assembly without automated clock/game driving, failover, GUI registration or real-process evidence. Next startup can compose protected profiles, node attachment, this assembly, creator initialization and join_device; don't reimplement the test's setup as fixture-only launch behavior.

Creator orchestration checkpoint: `device_join.rs::create_device` resolves an already published pending room, invokes only its advertised CreateRoom through the signed client, and requires a subsequent Lobby projection identifying the creator as connected coordinator before returning. Device-service integration now calls this operation rather than hand-scripting Create; the full mock test passes (0.68s). Non-mock `cargo check --locked -p poche-veilid --features device-service --offline` passed; broader mock library suite passed all 31 tests before this helper addition. Publication/service ownership, deadline policy, protected profile selection and GUI wiring remain incomplete. Do not expose/copy an invitation until creator initialization succeeds. Room-code wall-clock expiry is not the session InviteRecord revision expiry: production assembly must enforce the former on new admission at the service, not rely on the client-supplied decode time or the integration test's unlimited revision lifetime.

Service lifetime checkpoint: `VeilidDeviceService::serve` consumes a bounded callback receiver and processes at most four concurrent async handlers on the node runtime, so a cooperating capture need not serially block observation RPCs. `RunningDeviceService` retains the node and aborts async acceptance on drop; it does not provide replica persistence. Callback overflow uses nonblocking try_send and is a lost request, not success. The integration now uses owned nodes on both sides and this service task instead of a test-only serial loop. `cargo test --locked -p poche-veilid --features device-service,veilid-mock-test --offline --test device_service` passed (0.65s). A non-mock service check passed before adapting the callback receiver's Box type. This test proves registration on the owned runtime, not a load/fault bound under saturation, actual network traffic, menu Create or failover. Next assemble room publication and initial creator action with this task retained beside the room.

Join orchestration checkpoint: `device_join.rs::join_device` is an ordinary-worker operation returning a lifetime-owning PlayerDeviceClient and RoomId after code validation, route resolution, certified observation, admission/reconnect and confirmed connected membership. It never executes CreateRoom for an invitation. Existing members omit bearer proofs; new members supply the code-derived proof. A missing route can trigger signed rebind, then the advertised Reconnect action. Added explicit redacted Unavailable RPC reply because collapsing route loss into Denied prevented recovery. Device-service mock test now calls this function rather than manually scripting admission, then disconnects/drops/rejoins the same device and checks retained seat 1. Command `cargo test --locked -p poche-veilid --features device-service,veilid-mock-test --offline --test device_service` passed (0.64s). This is clean client reconstruction against a surviving in-process authority, not creator failover, process-kill persistence or GUI registration. Non-mock device-transport check passed before the final error-mapping addition; repeat during startup integration. Menu background owner must call this function with protected profile/node rather than reproduce its admission protocol.

Admission checkpoint: RoomCode now derives a domain-separated session bearer proof from its canonical secret code, binding admission to the complete invitation rather than a fixed player label. The RPC integration installs its hashed InviteRecord and action-source proof, admits an independently signed second player, denies a wrong secret, excludes occupied seat 0 and commits seat 1. `cargo test --locked -p poche-veilid --features device-service,veilid-mock-test --offline --test device_service` passed (0.43s); pure `--offline --lib room_code` passed five tests including proof round-trip and differing secrets. Test identities are fixtures, both device clients use one mock client node, and InviteRecord revision expiry is deliberately unbounded in this test. Production startup must set/enforce invitation lifetime and revocation; this does not prove two-process admission, protected profiles or GUI wiring. Next assemble startup/admission around these verified boundaries rather than introducing predetermined guest identities.

Node ownership checkpoint: `device_node.rs` starts Veilid on a dedicated ordinary thread that owns Tokio's runtime. Cloned owners retain the node; final drop signals asynchronous shutdown without blocking Bevy, while explicit worker-only `shutdown` waits for cleanup. Configuration/storage isolation and nonblocking callbacks remain caller responsibilities; startup does not implicitly attach to the public network. `VeilidDeviceTransport::from_node` retains the node after menu lifetime ends. `cargo test --locked -p poche-veilid --features device-transport,veilid-mock-test --offline --lib device_node` passed clone-survival and same-namespace clean restart. `cargo check --locked -p poche-veilid --features device-service --offline` passed without mocks. The device_service integration now uses this node owner and owning transport; its create/seat/disconnect/reconnect test passed with `--features device-service,veilid-mock-test --offline --test device_service` (0.30s). Still process-local simulation, not real sockets, crash recovery, admission persistence or launcher wiring. Next connect the menu's requests to startup/profile/admission, with explicit error and connecting feedback; do not confuse clean node restart with surviving game state.

Full client-path checkpoint: expanded device_service test to instantiate PlayerDeviceClient<VeilidDeviceTransport> on an ordinary worker with a resolved room. Through mock private-route callbacks it observes pending room, commits CreateRoom, takes seat 0, disconnects/rebinds the device route, observes reconnect-only actions, commits Reconnect, and regains ordinary actions. Fixed missing epoch 0 -> 1 transition after committed CreateRoom in VeilidDeviceTransport. `cargo test --locked -p poche-veilid --features device-service,veilid-mock-test --offline --test device_service` passed (0.31s runtime). This uses test-generated credentials and an in-process surviving authority: not process-kill recovery, creator failover, real sockets, GUI menus, or protected-profile persistence. Production startup/menu remains the next integration task.

Server RPC checkpoint: `VeilidDeviceService` dispatches through existing CertifiedDeviceRoom verification and replay boundaries, executes prepared cooperation outside the room mutex, and answers AppCalls using spawn_blocking for reducer work. `cargo test --locked -p poche-veilid --features device-service,veilid-mock-test --offline --test device_service` passed: valid signed observation returns, signed-room tampering denies, malformed envelope rejects, and a two-node private-route AppCall receives the observation via actual callback/service/reply APIs under mock simulation. This test does not use the synchronous VeilidDeviceTransport client yet or prove real sockets, menu, per-recipient reply cryptographic binding, pagination, epoch/route refresh, or failover. Next wire the real desktop startup/menu plus bounded runtime service and extend the full client test, without presenting this central CertifiedDeviceRoom adapter as completed decentralized recovery.

Client RPC checkpoint: added optional `device-transport` feature and `VeilidDeviceTransport<S>` implementing the existing DeviceTransport on a dedicated native worker with a live Tokio handle/resolved private route. It signs existing observe/invoke/route requests, carries signed cooperation, binds operations to the resolved room, and rejects wrong response variants and oversize/unknown-version envelopes. `cargo check -p poche-veilid --features device-transport --offline` passed (non-mock); `cargo test --locked -p poche-veilid --features device-transport,veilid-mock-test --offline --lib device_transport` passed envelope-negative tests. No server dispatch or GUI wiring yet. Next use the existing CertifiedDeviceRoom signature-verifying boundary (`device_client.rs`) for dispatch, with explicit recipient reply protection, AppCall integration tests, route refresh and epoch updates. Current 30KB bound fails oversized observations explicitly; chunked/trimmed-history protocol needed before full-history acceptance. These partial surfaces do NOT establish production identity/failover safety. Keep protocol decoding distinct from authorization.

Integration progress: root dependency now pins upstream 99c9616295f9195c97bf2f5f6b43f0d220ac5d8b (still versioned 0.5.7 upstream). Added non-default `veilid-mock-test`; migrated identity, membership and host-capability storage to async VeilidAPI secret methods. `cargo check --locked -p poche-veilid --features veilid-mock-test --offline` and separate non-mock `--features veilid` both passed. `cargo test --locked -p poche-veilid --features veilid-mock-test --offline --test mock_nodes` passed: two nodes share DHT, isolate device secrets, publish and resolve Poche rendezvous via real adapter in process-local simulation. This is not cross-process/live networking, durable OS keychain/restart evidence, or a completed DeviceTransport/GUI integration. Next: implement the device command/observation service and real-node lifecycle; keep mock out of desktop feature closure. Existing check-then-save identity race predates this migration and must be addressed before claiming concurrent profile creation safety.

Work: close G2 architecture portion; adapt poche-player-client/poche-veilid boundary without Bevy dependencies in protocol/core. Pin reviewed upstream mock-capable revision; isolate mock feature builds from real builds. Background service owns networking; typed proposals/observations cross the UI boundary. Read bevy_veilid only for task/event bridging inspiration.

Validation: `cargo test --locked -p poche-player-client -p poche-veilid`; add adapter tests for distinct devices, exact-recipient views, stale/duplicate actions, injected failures and recovery. Record actual mock-feature command once implemented. Separate opted-in two-process real-network acceptance from offline mock harness; respect existing public-network opt-in guards.

Complete when same adapter passes mock tests and actual two-process exchange, with topology documented. HTTP tests alone do not satisfy it.

### [ ] T3 Implement desktop menu and invitation contract

Windowless menu evidence: added `examples/menu_capture.rs` using run_menu and the existing image render target/screenshot mechanism, without network or a fake table. Acceptance timing now runs without NativeController and waits for screenshot completion with a bounded fail-safe. First capture (`target/phase6-menu.png`) was uniform gray: corrected explicit UiTargetCamera on menu and live-control roots. Second capture showed a missing default-font dash; shortened the clipboard label. Final `$env:WGPU_BACKEND='dx12'; cargo run --locked -p poche-native-ui --offline --example menu_capture -- D:\Repos\Games\poche-3\target\phase6-menu-verified.png` passed fresh-path/nonuniform-image checks; inspected with view_image: centered readable title, name field, Create, clipboard button, invitation editor and Join. No visible window was opened. All 17 native lib tests passed. This proves actual menu pixels only, not keyboard/pointer input or successful live transition. The harness rejects existing output paths, so choose a new filename on reruns. Menu capture is pre-admission local developer evidence, not an authenticated remote device capture request.

CLI wiring checkpoint: ordinary `poche desktop launch` now calls run_menu; explicit room/fixture/capture launch paths remain. New desktop/connection.rs connects menu requests to protected local profiles, per-profile node storage/exclusive instance lock, guarded vault transport password, public readiness, publish/create or join, and NativeLiveDevice. Distinct names select independent hashed local profile labels; same active local profile is currently rejected (not completed sibling-device enrollment). Publication/service/lease owners remain in the connection worker for app lifetime. Live menu-mode UI exposes advertised actions and copy invitation; Leave/Close require a repeat confirmation click. Native action IDs now use a per-connection random namespace, truncated to protocol bounds after the unit test caught an oversized first attempt. `cargo check -p poche-cli --offline` passed, creator uses its own service node in the passing mock integration (0.73s), and all 17 native lib tests passed after the ID correction. No visual launch or real protected-node acceptance has occurred. Next: windowless menu input/capture and protected two-process startup; verify menu-to-lobby spatial projection (including empty seats), invitation copying and action delivery. Countdown/game clock driving, display-name propagation, graceful leave/closed screens, service/publication cleanup on failed startup and creator failover remain incomplete. Do not equate compiling default wiring with playable acceptance.

Bevy connection bridge checkpoint: run_menu now starts without a fixture controller/table, installs DesktopConnectionWorker, and waits for a confirmed live device before creating the native scene in the same app. Worker requests/results are bounded; connection work runs on an ordinary thread and returns static redacted errors to menu status. A stopped worker no longer leaves busy indefinitely. Menu camera uses the configured render target. `cargo check --locked -p poche-native-ui --offline` passed; native lib suite passed 17 tests; final focused desktop_menu rerun passed all 3 after stopped-worker handling. Failure test checks connector runs on another thread, status becomes actionable and no fixture controller is installed. Successful GPU/menu input transition and menu-only windowless capture/exit remain unverified. CLI default still uses legacy launch flow: next wire a protected-profile/Veilid connector into run_menu and retain publication/service owners for the room, then exercise actual Create/Join visually rather than declaring this API sufficient.

Menu preparation checkpoint: `crates/poche-native-ui/src/desktop_menu.rs` adds a Bevy-native name/Create/Join surface, bounded editable invitation input, shape-validator callback, explicit Join and busy/status feedback. Clipboard bytes are not logged; unrelated clipboard data leaves existing invitation text untouched; valid clipboard data does not initiate a connection. Native crate now explicitly enables Bevy's `ui` feature alongside workspace `3d`; `cargo check -p poche-native-ui --offline` passed. The plugin is not registered in the desktop launcher yet: the current launcher still uses a fixture or HTTP live-device path. It emits typed requests but has no production connection owner, invitation parser binding or room-transition wiring. Do not present this as a working menu or two-process acceptance. `cargo test --locked -p poche-native-ui --offline --lib` passed all 16 tests, including clipboard refusal/prefill and explicit Join/duplicate-press suppression. These inject ECS interactions/clipboard results; GPU menu capture/input and actual OS clipboard acceptance remain pending. Next integration must keep credential loading/network startup on a background owner and retain that owner after transition to the live table.

Work: name/profile selection, Create, Join, Copy, shape-gated clipboard prefill, explicit Join, connecting/error states. Invitation distinct from player recovery secret. Validate length/version/encoding before network use. Two instances can select independent profiles without accidentally sharing identity.

Validation: `cargo test --locked -p poche-native-ui -p poche-player-client`; parser tests for malformed/oversized/unsupported codes; UI-input puppet tests for create/copy/prefill/join and clipboard refusal without secret logging.

Complete when two game instances join through the presented menu, not a privileged fixture route.

### [ ] T4 Ground lobby and private-hand interactions

Strict desktop hand policy: `publish_device_room` now selects `OracleRoomActionSource::without_hand_sharing()` before room construction. It excludes RequestHand/GrantHand at the authority's advertised-action validation boundary, not only in Bevy; legacy callers keep their prior sharing experiments. It is not a runtime revocation switch for existing grants. The extended certified-service regression admits a spectator after dealing, verifies no own/granted hands or sharing actions, then sends a handcrafted correctly signed RequestHand RPC and requires UnknownAction with unchanged revision and no granted hand. Initial broad is_err check was tightened and exposed duplicate test enrollment; enrolling through CertifiedDeviceRoom fixed the test so it now proves the policy boundary. Focused regression passed; assembled mock Veilid device-service test passed (3.95s). End-to-end pose/reveal security, formal-policy coverage and rendered privacy evidence remain pending.

Native projection refresh (`ca4ac98`): changed live projections rebuild viewer-scoped roots, text, materials and drag authority while preserving camera/render target/lighting. Renderer-world regression verifies removed cards do not survive and the camera does; 19 native library tests passed. Current whole-scene rebuilding is not optimized pose streaming or live two-process input evidence.

Unseated-viewer correction: native_controller_from_observation previously rejected every viewer without a seat, including a newly created lobby. NativeController now represents issuing_seat as Option<SeatId>; existing seated constructor delegates to try_new_viewer. Unseated viewers receive no first-owned card, cannot commit named/drag plays, and get lobby/spectator guidance instead of card-play instructions. Native regression executes CreateRoom -> TakeSeat -> ReleaseSeat through the device client and constructs the projection at each step, verifying optional seat authority and unseated play denial. All 18 native lib tests passed; final CLI check passed after guidance-text adjustment. This fixes the data-path blocker but is not yet a captured successful menu-to-lobby transition, spectator capsule implementation or pointer-input acceptance.

Work: close G3/G4. Table, seat claims, spectator capsules, local camera, ready/deal/actions as agreed. Card views share logical identity; render-layer proxies allowed. Authority checks movement/reveal/spawn separately. Render back-only opponent observations; never send faces and hide them with camera layers. Preserve action palette/CLI access beside spatial affordances.

Validation: `cargo test --locked -p poche-session -p poche-runtime -p poche-spatial -p poche-native-ui`; `cargo run --locked -p poche-xtask -- coverage audit --all`. Add seat-race, illegal spawn/reveal, privacy and UI action tests. Update formal models/coverage for changed semantics with explicit scopes; screenshots do not prove secrecy.

Complete when two windows agree on seating/public state while private hands and actions obey policy; actual input and captured output meet G4 endpoint.

### [ ] T4a Separate shared poses from logical rules transitions

Play-transition integration evidence: Veilid mock `device_service` test now completes both bids using advertised actions, claims/moves the next legal card, submits its play through the real reducer, and verifies identical opaque ID/full pose in `physical_public` with that ID absent from `physical_hands`. Public pose list was empty before play. `cargo test --locked -p poche-veilid --features device-service,veilid-mock-test --offline --test device_service` passed in 4.42 s. This proves current-trick publication through the signed service path; completed tricks/deck, graphical live play and crash recovery remain unfinished.

Current-trick publication: optional `DeviceObservation::physical_public` is separate from private hand metadata. Runtime publishes retained poses only for faces already in the public current trick and only for the current deal epoch; client structural validation checks this public membership. Native Play-card overlays consume these poses. Hand pose write authorization is unchanged. Player-client (8) and runtime (35) library tests passed; native tests compile. Dedicated play-transition/privacy regression and live visual evidence are still required. Completed-trick/deck publication is not implemented, so full card lifecycle remains incomplete.

Retention regression: extracted the accepted-write storage boundary and added `accepted_pose_retention_is_deal_scoped_not_hand_scoped`. It proves same-epoch writes preserve earlier entries and a changed deal epoch clears them; runtime suite passed 35 tests. The test uses synthetic card keys, not an actual game play transition, and must not be cited as end-to-end lifecycle proof. Next lifecycle work requires a distinct public played-card observation path without weakening private hand authorization.

Authority pose retention correction: accepted pose storage now tracks the deal identity epoch (game-start revision plus round), retaining poses after cards leave hands instead of pruning by current hand membership on every write. A successfully accepted new-epoch write clears old-epoch entries; rejected writes do not clear them. Storage remains bounded by the deck's identities. `cargo test --locked -p poche-runtime --offline --lib` passed 34 tests; Veilid `device-service,veilid-mock-test` device_service integration passed in 4.11 s. Existing tests cover pose authorization/read delivery, not yet a played-card retention regression. Publication/rendering of played/deck poses and crash persistence remain unfinished; this is not full lifecycle acceptance.

Adaptive default-hand framing: hand camera distance now derives from 72 mm card spacing, 64×88 mm card footprint, 20% margin, viewport aspect and actual perspective FOV; it does not chase moved cards outside the hand area. Native suite passed 27 tests, including one-through-seven-card width/height fit at aspect ratios 0.5, 1 and 4.8. This is frustum arithmetic evidence; resized graphical/pointer acceptance and lifecycle work remain pending.

Cross-camera anchor correction: Drag now integrates each current-camera pointer delta into the retained local world position rather than reprojecting cumulative drag distance through a different camera. Delayed network transforms do not reset the gesture anchor. New pure ray regression covers a camera change with zero motion and subsequent incremental movement. This proves coordinate continuity mathematically, not picking across viewport boundaries, cursor/card alignment ergonomics, or live end-to-end input. Those acceptance checks remain required.

Hand readability correction: native physical defaults space 64 mm cards at 72 mm centres rather than canonical logical packing's 14 mm centres. Opaque-order mapping applies the same defaults to owner/peer; accepted poses override them. Logical scene/zone membership is unchanged. Windowless `table_capture` outputs `target/phase6-hand-camera-spaced.png` and `target/phase6-hand-camera-upright.png` were inspected: separate labels now readable, final camera up direction corrected to -Z. Native suite passed 25 tests before the final camera orientation correction; final capture compiled/ran successfully after it. Adaptive framing for larger hands, cross-camera continuity and full live acceptance remain pending.

Windowless hand-camera inspection: added `examples/table_capture.rs` (fresh path required, windowless image target, rejects uniform output). Ran with `WGPU_BACKEND=dx12; cargo run --locked -p poche-native-ui --offline --example table_capture -- <fresh PNG>`. Inspected `target/phase6-hand-camera-first.png`: hand tiny/dark. Extended directional-light render layers to include hand layer and moved hand camera from 0.45 m to 0.15 m; rerun `target/phase6-hand-camera-lit.png` completed and was inspected. Cards are larger/lit but overlap and labels collide; NOT acceptable hand readability. Next: fix physical hand arrangement/card label placement, adaptive framing and cross-camera drag continuity, then rerender. Fixture capture does not prove live Veilid/pointer interaction.

Hand camera foundation: native setup creates a second camera on the same render target; a resize-aware inset renders only owned hand meshes and their text via render layer 1, sharing entities/transforms with the tabletop. It deactivates when no owned hand exists. Drag ray selection chooses the highest-order active camera containing the pointer. Native library suite passed 25 tests; this does not exercise the new viewport visually. Next required: inspect windowless capture, verify lighting/text/picking, anchor continuity when crossing viewports (current start-ray reconstruction changes camera), and add focused viewport/interaction tests. No G4 completion claim.

Received pose validation: `PhysicalPoseState::validate` checks device identifier, positive lease generation/sequence, coordinates within ±10,000 mm and canonical rotation components in [0,360000). `DeviceObservation::validate_for` rejects malformed pose metadata before render consumption. Player-client library suite passed eight tests including exact boundaries, extreme integers, rotation range and zero counters. This is structural validation only; cryptographic and authority checks remain separate. Full graphical/network lifecycle acceptance is still pending.

Initial identity placement correction: `NativeController::physical_defaults` maps each visible card to its opaque-ID-order slot, matching the hidden peer's slot association. These are render defaults, not fabricated signed pose receipts; canonical logical order and private faces remain unchanged. Native library suite passed 25 tests, including reversed face/identity ordering compared between owner and peer. This resolves the previously noted default-order mismatch for current hand contents. It does not yet preserve vacated slots across card removal or retain deck/play poses; full lifecycle and inspected render acceptance remain required.

Rotation input checkpoint: native live Drag now accumulates normalized full YXZ rotation per gesture, with horizontal motion at 0.5 degrees/pixel and Shift (yaw), Ctrl (pitch), Alt (roll), including combinations. Ordinary unmodified dragging preserves rotation. The live action area includes a controls hint. Local accumulation avoids losing rotation to delayed accepted-pose feedback; all samples use the same authorized/coalesced pose path. Native library tests passed 24 tests including axis selection, wraparound and accumulation. This is input-math evidence, not an inspected pointer/render test; modifier ergonomics, camera interactions, cross-viewport behavior and final acceptance remain pending.

Real-network native motion evidence: extended `crates/poche-cli/src/cli/desktop/process_probe.rs` so the joiner calls `NativeLiveDevice::submit_pose` after dealing, awaits its receipt, and the independent creator process observes the exact position `[170,240,-310]` and YXZ millidegree rotation `[45000,12000,270000]` on a face-hidden card. Both assert unchanged logical revision and public game state. Elevated `POCHE_ALLOW_VEILID_PUBLIC_TEST=I_ACCEPT_PUBLIC_NETWORK_TRAFFIC; cargo test --locked -p poche-cli --offline protected_desktop_two_process -- --ignored --nocapture` passed in 25.46 s (2026-09-09, fresh protected profile suffix 1788984798286). Both child processes ran windowless; no graphics or pointer events were exercised. Protected test profiles remain; invitation temporary files are parent-owned and removed by TempDir. This proves actual Veilid/native-worker pose delivery, not continuous drag latency, cross-camera interaction, lifecycle recovery or visual correctness; those remain required.

Backpressure follow-up: `MotionOutbox` now coalesces consecutive unsent samples for the same card, retaining the latest full pose and any unsent explicit claim. The bounded worker queue is retried nonblockingly by later UI polls, including after DragEnd; logical action submission cannot overtake pending poses and currently reports retry-required rather than queuing that action. `cargo test --locked -p poche-native-ui --offline --lib` passed 23 tests, including saturated-channel/final-sample/claim preservation; `git diff --check` passed. This proves local queue retention, not network acknowledgement/reconnect delivery. The earlier backpressure gap below is narrowed by this checkpoint; real pointer/network acceptance, rotation input and remaining G4 work are still pending.

Native pointer wiring checkpoint: owned live hand-card Drag events now resolve the tabletop camera ray into a horizontal world plane at the original card height, preserving the grab offset across accepted-pose refreshes. They queue an explicit initial claim and subsequent physical updates regardless of legal-play availability. DragEnd resets only local gesture bookkeeping; accepted physical poses remain separate from the existing drop-to-Play action. Worker reads are forced between accepted pose writes so uninterrupted samples cannot starve snapshots. `cargo test --locked -p poche-native-ui --offline --lib` passed 22 tests, including a new pure ray/offset/parallel-plane test; `git diff --check` passed. This does not prove pointer-to-network integration or smoothness. Remaining: motion coalescing/final-sample delivery under backpressure (the current bounded queue reports busy), rotation input, hand camera/cross-camera drag, full card lifecycle pose retention, and actual two-client input/capture evidence. Do not mark G4 complete from these unit tests.

Native submission checkpoint: `NativeLiveDevice::submit_pose` queues explicit claims and position/full rotation samples onto the existing bounded worker channel. The worker derives generation/sequence from its latest accepted hand metadata, uses the latest observed session epoch, rejects ordinary updates without this device's lease, and submits through `PlayerDeviceClient::physical_pose` without blocking Bevy. Successful receipts force an observation refresh without inventing a logical revision; rejected writes are reported and never automatically retried/taken over. `cargo check --locked -p poche-native-ui --offline` and `cargo test --locked -p poche-native-ui --offline --lib` passed (21 tests). These existing tests do not exercise the new submission path end-to-end. Next: wire drag handlers, add worker/lease integration tests and bounded motion coalescing, then prove cross-client motion; this checkpoint does not satisfy G4.

Native pose consumption: periodic worker reads now use snapshots and compare physical_hands as well as logical revision, since revision-only waits necessarily miss pose-only changes. Unchanged snapshots do not publish render events. NativeController keeps a separate pose overlay; owned cards resolve by entitled face and hidden cards by the opaque-ID-ordered seat list. Applying overlays uses full YXZ rotation and does not mutate the canonical scene. Focused ECS transform regression passed and also checks hidden-card pose association without face changes. This is not inspected graphical/network evidence. Default unposed card arrangement still needs a consistent physical-ID mapping across views; input submission, cross-camera dragging, retained deck/play poses and live-network motion remain required. Pose read polling currently uses the existing 50 ms healthy cadence; rate/coalescing and transport-latency acceptance are still pending.

Signed hand-pose channel: PhysicalPoseRequest signs its certificate, room/session, opaque card ID, CAS lease generation, claim/update sequence, bounded integer position and full normalized yaw/pitch/roll under a distinct physical-pose-v1 domain. DeviceTransport has a fail-closed optional method; Veilid signs/dispatches it through CertifiedDeviceRoom. Authority verifies certificate/signature/capability/epoch, exact device route connectivity, current connected hand ownership, bounds, and lease/sequence; it does NOT require the player's turn or execute Play. Claims advance the expected generation and updates require its owning device. Accepted state appears in physical_hands.pose without changing logical revision/game state. Legacy HTTP/loopback transport methods are unsupported rather than silently bypassing checks. CLI tests check compiles. Mock Veilid integration exercises claim/move, back-only peer observation, unchanged game state/revision, stale sample, out-of-bounds position and opponent takeover rejection. This remains hand-only pose storage; current pruning drops entries no longer in hands, so full play/deck pose lifecycle MUST be extended before G4 acceptance. Native polling still needs to detect pose-only updates, renderer/input/CLI controls need wiring, and real-network motion/capture/recovery evidence is pending. No full shared-motion completion claim.

Hand-identity observation integration: production room assembly generates independent 32-byte entropy and enables identities before enrollment. Runtime supplements DeviceObservation with optional physical_hands (opaque ID/seat/owner-only optional face), sorted by ID rather than hidden face order. Legacy observations omit the empty field. Identity generation includes committed game-start revision and round index, so lobby resets do not reuse known identities. Enabled running snapshots without game-start provenance fail closed; recovery must preserve this provenance and identity secret. Client validation bounds/uniqueness-checks metadata and rejects faces outside the connected viewer's own hand. Extended runtime test confirms spectator and owner see identical IDs with different face entitlement and rejects a forged spectator face. `cargo test --locked -p poche-player-client -p poche-runtime --offline --lib` passed 7+34 tests; mock device-service integration passed (3.91s); CLI tests check compiled. This covers HAND identity metadata only, not full deck/play identity lifecycle, pose transport, rendering or secret replication. Those remain required, not implicitly satisfied by metadata.

Physical identity prerequisite: source inspection found `realization.rs::push_card` assigns IDs from output length; removing a hand card shifts later scene ordinals. Added authority-only PhysicalDeckIdentity mapping and opaque 256-bit PhysicalCardId using domain-separated keyed BLAKE3 from independent secret entropy plus round epoch (must NOT use the public/predictable game seed). No Debug/serialization of the face mapping. CardManipulation now permits a generic identity with the old CardObjectId default; PhysicalCardManipulation keys the same pose reducer by round identity. `cargo test --locked -p poche-spatial --offline` passed all 32 tests, including stability across reordered/removed hand entries, retained moved pose, uniqueness in the deck, and changes across secret/round. These tests are not a cryptographic proof. Mapping is NOT yet generated by the room, replicated for recovery, included in viewer projections, or connected to network motion. Next must bind authoritative logical cards to these IDs and transport recipient-safe pose records; do not attach persistent poses to legacy output ordinals.

Work: audit current poche-spatial refinement assumptions before changing them: geometry must no longer require logical location to follow every pose. Keep stable opaque card IDs, logical location (including deck index), and separately authorized world position/rotation. A pose update is not a bid/play/ownership change. Check manipulation permission independently from turn legality; disallow moving opponents' cards absent a grant. Pose traffic needs per-object/device ordering, bounded rate/coalescing, finite bounded coordinates and a convergent latest accepted pose; do not route every mouse sample through the scoring log. Record final accepted poses for reconnect. Resolve competing devices through explicit manipulation ownership/ordering, not packet-arrival accidents.

Source inspection note: current `PoseMm` in `units.rs` supports yaw only. Introduce an explicit full-orientation manipulation representation rather than silently interpreting the user's rotation requirement as yaw alone; preserve v1 fixtures through an adapter/version boundary. `abstract_viewer_scene` already reads typed card locations but invokes scene validation: audit those validation assumptions before overlaying arbitrary permitted poses.

Initial implementation (while T1 build runs): added `manipulation.rs` physical overlay with full yaw/pitch/roll, bounded position, monotonic per-lease sample sequence, explicit lease changes and play-region entry edges. It stores no card face/logical location and does not authenticate network senders: authorization must remain in the eventual transport/session integration. Added three unit tests for rejection atomicity, stale writers/samples, and no delayed automatic play. Not yet integrated into GUI/network; T4a remains incomplete. Spatial test command is waiting on the baseline build lock (session 19407); native baseline session 80091 has reached native UI compilation. Reference Bevy checkout now detached at b56fc29d3016e641754765244b5ba3f9cc504671 with untracked assets preserved.

Validation: `cargo test --locked -p poche-spatial -p poche-session -p poche-runtime -p poche-player-client`; add invariants that pose-only actions leave logical rules state unchanged; stale/out-of-order packets cannot revert the settled pose; unauthorized poses are rejected. Two-client puppet wiggles position AND rotation during another player's turn and observes motion before drag release, with the remote side receiving only a back/opaque identity. Test convergence after reconnect.

Complete when physical motion is shared without turn advancement, card reveal, ownership change or mutation of the rules-engine state. Update formal abstractions to represent pose-only stuttering of logical state; bounded geometry models and input/render tests provide separate evidence, not a proof of arbitrary continuous geometry.

### [ ] T4b Bridge viewports and attempt play separately

Work: use the hand and table cameras to project/unproject onto the same world with stable drag ownership and offset across the viewport boundary. Proxies may render the same card but must not create another logical object. Entering the play region creates a uniquely identified play attempt tied to the relevant rules revision; define edge-trigger/rearm behavior so holding a card there does not spam commands or automatically play it when the turn later changes. Proposed MVP default (from user's tentative example): an authorized pose may stay in the play area after rejection while the card logically stays in hand; visibly label 'not played' and offer return-to-hand. Face disclosure occurs only with accepted authorized reveal, even if a private card is rotated or moved to the center.

Validation: `cargo test --locked -p poche-native-ui -p poche-spatial -p poche-runtime`; puppet actual viewport crossing, rotation, legal play, out-of-turn denial and repeated region entry. Compare both clients' poses and logical states; inspect screenshots of denied versus accepted play. Verify no automatic delayed play when actor changes and no secret face in remote payload/capture.

Complete when both views show the same authorized world motion; only accepted rules commands change logical location. The hand camera may keep showing a logically owned but physically displaced card through a clearly identified proxy; document chosen representation rather than silently snapping it back.

### [ ] T5 Implement crash/rejoin identity and state recovery

Real no-peer restart evidence: elevated `POCHE_ALLOW_VEILID_PUBLIC_TEST=I_ACCEPT_PUBLIC_NETWORK_TRAFFIC; cargo test --locked -p poche-cli --offline protected_desktop_empty_room_disbands -- --ignored --nocapture` passed in 31.81 s. Two sequential hidden processes use the same protected profile; the sole creator exits, then Create receives a different invitation after the authenticated no-peer checkpoint is terminalized. An observation read retry occurred. This is sole-member shutdown/replacement evidence, not all-peer loss of an established multi-player game, explicit coordinator departure, stale-code rejection on another device or expiry under failed storage. Those remain pending.

Terminal startup handling: a valid creator-owned checkpoint with no remaining peer is converted to an encrypted disband marker before Create may publish a new lobby; corrupt/mismatched snapshots are not interpreted as empty. Certified recovery now records explicit Closed state, and desktop envelope encoding turns it into the terminal marker, so explicit close cannot leave an apparently recoverable active slot. Focused closed-checkpoint and certified replay tests pass; locked CLI check passes. These are implementation/unit checks, NOT completed no-peer or final-leave end-to-end evidence. Legacy pre-marker closed snapshots still fail closed. Coordinator departure semantics and all-peer loss tests remain pending.

Real creator crash evidence: `POCHE_ALLOW_VEILID_PUBLIC_TEST=I_ACCEPT_PUBLIC_NETWORK_TRAFFIC; cargo test --locked -p poche-cli --offline protected_desktop_creator_crash -- --ignored --nocapture` passed elevated in 53.28 s, profile suffix 1788991301437. Hidden parent kills the exact creator child after peer pose acceptance, keeps the peer polling, restarts the original creator profile via Create, and verifies original invitation, seat 0 and a hash of own hand/public game/physical hands against the pre-crash state. Peer retains the same public game. Read retries occurred; no uncertain writes are retried. This proves protected same-device creator restart with surviving peer, NOT creator-independent replica state, all-peer disband, GPU/input behavior or recovery after arbitrary write timing. Harness also retains the earlier participant-crash variant. Next complete lifecycle expiry/final-departure cases and remaining replica/graphical acceptance rather than marking T5 complete from this one success.

Desktop restart wiring: Create now reads `active-room` before publishing. A valid nonterminal checkpoint enters `connection/recovery.rs`, reconstructs a membership-gated service, retains protected checkpoint/expiry writers, resumes original DHT ownership, waits for a peer, then joins as the existing creator profile. Sixty seconds is the initial reversible production recovery grace (65-second worker safety bound). Terminal markers permit a fresh room; corrupt/noneligible snapshots fail rather than silently replacing them. Failure after route resume stops the handler and releases the resumed route. Locked CLI check and four mock device-service tests pass; creator-kill production test has NOT run. Next extend the process harness to kill creator while peer continues polling, then compare seat/hand/pose/history after restart. All-peer shutdown, no-peer snapshot handling, persistence-failure retry lifetime, UI feedback and independent replica evidence remain unfinished.

Survivor route discovery: observation transport failures now trigger a bounded DHT route refresh followed by retry of the same signed read only. `refresh_resolved_room` binds room/host/network/session, forbids route epoch rollback and expired records, and releases the prior imported route after replacement. Mock-node test passed after rotating the host route and refreshing the existing resolved client; expiry rejected. This is route discovery evidence, not a full creator crash or successful public-network rerun. Startup restoration still pending; do not retry uncertain game writes. The extra refresh happens only after the original three bounded read attempts fail.

Expiry persistence boundary: recovered-service assembly now requires a terminal writer. Scheduler or dispatch detecting an expired survivor gate freezes normal saves, retries a failed terminal callback, and stops rewriting after success across clones. `DesktopRoomDisbanded` is a typed private marker rejected by ordinary room decoding. Four device-service tests pass (5.01 s), including failed-first/successful-second terminal callback; focused genesis test confirms marker cannot restore as a room. The actual encrypted expiry writer and route withdrawal are NOT yet wired into desktop restart. A crashed process before expiry persistence and rollback of older storage still need lifecycle handling; this callback test is not end-to-end disband evidence.

Recovered-service assembly: `DesktopRoomGenesis::recovered_service` now reconstructs the room, requires the recorded coordinator principal, derives peers from reducer membership (not enrolled device/service profiles), rejects no-peer recovery, attaches persistence and the fresh survivor gate without publishing a route. Focused runtime `certified_authority_services` test verifies enrollment-only exclusion, joined-peer inclusion and guest-as-creator rejection; `desktop_genesis` verifies invalid recovery does not reach the writer. Both pass. Actual desktop startup, expiry tombstones, route rotation and independent peer state evidence remain pending; this constructor alone does not establish live recovery.

Recovery dispatch handshake: optional `awaiting_survivor` service mode suppresses scheduler and non-observation requests until a configured surviving principal answers the fresh correlation challenge through normal certified signature validation. Snapshot clients re-sign once; wait clients use the snapshot handshake. Three device-service tests pass (5.10 s), including unchanged old read staying gated, tampered challenge signature denied, fresh signed response opening the gate; locked CLI check passes. Startup has not enabled this mode yet. Expiry returns unavailable but does NOT yet persist a disband tombstone or withdraw a route. Eligible members must be derived from the authenticated recovered room, not arbitrary network requests. This does not supply the separate replicated-state evidence required by G2.

Survivor challenge primitive: `recovery_presence.rs` issues a fresh 192-bit correlation challenge for each restart, excludes the creator and nonmembers, and accepts only matching authenticated peer evidence before a supplied grace deadline. Focused `recovery_presence` test passed (creator/outsider/old-challenge/deadline rejection). Caller must verify the signature and authorization before calling it; the helper is not integrated into dispatch yet and is not consensus, replicated-state evidence or an automatic disband mechanism. Next wire a challenge-bearing response and signed peer retry into recovery-mode dispatch, stop mutations/scheduler until the gate is satisfied, and persist terminal expiry before any subsequent restart. Do not use cached signed observations as proof of survival. Sixty seconds is used by the test, not yet a finalized production timeout.

Recovery discovery data: desktop writes now target the profile-scoped encrypted `active-room` slot, and genesis retains the original invitation. Decoding checks its admission proof against genesis; the focused `desktop_genesis` test passes including mismatched-proof rejection, and locked CLI check passes. Earlier per-room experimental files are untouched; no migration or fallback to them is implemented. Startup still must consume this slot before creating a replacement, and must not resurrect a closed/all-expired room. `resume_host_room` restores only DHT ownership/route and explicitly cannot recreate an invitation, which is why the invitation is retained inside encrypted storage rather than public metadata.

Production save wiring: desktop creation now calls `publish_durable_device_room`; its writer encodes genesis plus certified recovery into a zeroizing buffer and saves via protected authority storage, keyed by local profile and room ID. Startup save failure prevents service startup; redacted storage error categories are logged. `cargo check -p poche-cli --offline` passed. Public two-process validation was attempted twice: first failed on action observation transport unavailability (38.71 s), second on lobby publication `Unavailable` (13.01 s); neither proves this updated production path. Mock service integration now uses the durable publisher and exercises its writer. Saved-room discovery/invitation retention, restore startup, survivor/disband checks and creator crash validation are still pending. Do not claim full recovery from these writes alone.

Desktop genesis envelope: `device_room.rs` now uses `DesktopRoomGenesis` for fresh room construction and provides authenticated-storage encode/decode with certified recovery state. The fixed v1 configuration retains seed, admission proof, clock/environment identities and strict hand-sharing policy. Focused `desktop_genesis` library test passes, rejecting unsupported version and mismatched room; private state compares equal except newly allocated local route IDs, whose counter remains monotonic. No private assertion dumps remain. This is configuration/serialization evidence, not a restarted creator or route publication. Next: give the desktop publisher an encrypted writer using this envelope and recover its saved invitation/route only after survivor/disband checks.

Service persistence boundary: `VeilidDeviceService::with_recovery_sink` saves an initial certified checkpoint, then saves under the room mutex before returning each dispatch result and after scheduler work. Save failure latches an unavailable state shared by all service clones; it does not expose later unsaved projections or silently resume. `cargo test --locked -p poche-veilid --features device-service,veilid-mock-test --offline --test device_service` passed 2 tests (4.55 s): persistence ordering/failure latching and existing mock game/motion flow. This optional hook is not enabled in the production connector yet. It currently saves even idle scheduler/read metadata: optimize/coalesce only where identifier and replay durability remain correct before enabling it at normal polling rates. The remaining production envelope must include genesis/configuration; survivor/disband policy and creator recovery remain mandatory, not replaced by this hook.

Serializable recovery boundary: runtime now exposes versioned authority journal, adapter and certified-room recovery records (no Debug, private storage only). They preserve transport counters, profile/connection metadata, physical identity secret/poses, projection sequence and exact signed read/write/route caches. The enclosing production room format must still supply identical genesis and action-source configuration; no production storage wiring is claimed. Runtime library suite passed 40 tests; full-game integration passed using the versioned journal; adapter test serializes a pose and rejects unsupported schema; certified replay test now serializes before retrying a committed action and old observation. Next concrete integration: add the production room genesis/config envelope, connect save-before-ack to encrypted storage, and restore only after the survivor/disband decision. Default chat-tail replay is the tested path; arbitrary custom chat capacity and rollback safety remain unproven.

Storage validation environment: the final sandbox rerun failed only the existing Windows credential-vault probe with `ProtectedStoreUnavailable`; rerunning the same command elevated passed all 17 tests. The new checkpoint cases use an in-memory test vault and real temporary encrypted files; production key custody still uses the OS vault.

Protected storage foundation: `poche-player-client/src/protected_store/recovery.rs` now provides authority checkpoint save/load with XChaCha20-Poly1305, a separate OS-vault key, fresh nonces, profile/room authenticated scope, 32 MiB plaintext bound, zeroizing decrypted buffers and synced temporary-file replacement. `cargo test --locked -p poche-player-client --features protected-store --offline --lib` passed 17 tests, including encrypted replacement, tampering/relabel rejection and missing-key fail-closed cases using the test vault. This is not yet wired into the room service and does not prove power-loss durability, rollback protection or creator recovery. Next: encode versioned complete service recovery material (genesis/journal, profiles, physical identity/poses and replay state), persist before acknowledgement, then validate survivor/disband policy before resuming a route. Preserve the existing creator-crash and all-peer-disband acceptance requirements.

Authority journal prototype: private journal records authenticated command/disconnect ingress plus clock advances (including pre-generation delivery counter); generated clock commands are not double-recorded. Serializable `AuthorityJournalInput` deliberately lacks Debug and is documented authority-only because commands may contain hidden deal material. Trusted replay establishes local recipient routes and reapplies inputs. Runtime suite passed 40 tests; focused Create+clock JSON round-trip matches state, clock, journal and committed events. Full-round/transport-loss replay equivalence, bounded/versioned authenticated storage, genesis data, physical/service records and durable-before-ack integration remain required. Not exposed through a player export or CLI, and not production recovery.

Durable journal boundary audit: `InProcessAuthority::drive_one` receives authenticated Command/Disconnected inputs, but `advance_clock_to` independently generates commands and calls `process_command`; capturing only transport ingress omits clock-driven transitions. The private `process_command/process_disconnect` boundaries cover reducer mutations, while clock scheduling/counters and physical pose writes require additional records. A production journal must preserve initial protected configuration plus exact commands/principal bindings, transport observations, clock progression, physical identity/poses and certified replay metadata; replay must not regenerate clock commands twice. Existing FixtureInput remains unsuitable as-is. Next implementation should establish this complete versioned record contract and a full-round replay equivalence test before durable-before-ack wiring. No journal implementation or persistence is claimed by this audit.

Full-game journal evidence: `cargo test --locked -p poche-runtime --offline --test in_process_full_game` passed. The three-client acceptance scenario serializes authority recovery inputs and replays from genesis across actual countdown expiry and the complete two-player deal/bid/play/scoring schedule; final reducer state, committed events and journal match exactly. The accessor is explicitly authority-private material (hidden deals), not a player-safe transcript. Durable encryption, write-before-ack, service/physical state restoration, creator loss and disband remain pending.

Closed-state restore guard: runtime adapter restore rejects `SessionPhase::Closed` before rebuilding routes, including when called through certified restore. Runtime suite passed 40 tests including a closed-checkpoint negative test. This protects only an explicitly closed snapshot; it does not solve stale older snapshots, all-peer expiry, persistent tombstones or route withdrawal. Encoding audit: SessionState/SessionEvent/Oracle game types currently lack a durable serialization contract; existing FixtureInput is test vocabulary, not a production certified journal. Do not treat opaque in-memory checkpoints as persisted recovery.

Transport identity continuity: authority checkpoint now retains next connection/observation counters and restore takes their maximum with supplied transport counters. This prevents fresh routes or disconnect observations from reusing pre-restore IDs. Runtime suite passed 39 tests including disconnect-before/after-restore identity regression. No durable persistence/process recovery claim; queued transport ingress is still intentionally not checkpointed and durable-before-ack handling remains required.

Restore route-status correction: runtime checkpoint records each enrolled device's route-connected bit. Fresh route reconstruction preserves that bit without generating new disconnect ingress; previously ordinary enrollment would silently reconnect offline routes. Runtime suite passed 38 tests including closed-route/no-new-ingress regression and existing certified restore/replay tests. Persisted connected bits are not proof of current peer liveness; surviving-peer confirmation and expiry/disband policy remain required before actual process recovery.

Certified checkpoint foundation: opaque `CertifiedRoomCheckpoint` pairs the runtime checkpoint with enrolled profiles, exact observation/action/route replay caches, service profiles and next service command. Restore resets the process-local countdown elapsed anchor (does not reuse an old process clock). Existing signed replay test now crosses restore: exact action/observation responses survive, revision stays 1, changed signed retry remains denied. Runtime suite passed 37 tests. This is still in-memory only and does not prove durable encoding, persistence-before-ack, deadline policy, revoked capture handling, creator failover or disband safety.

Runtime adapter checkpoint foundation: opaque `RuntimeDeviceCheckpoint` retains authority checkpoint, profiles, cloned action source, projection sequence, physical secret/poses/epoch and cooperation time. Restore creates new transport routes through enrollment and requires capture providers to register again. Existing runtime suite passed 36 tests before adding the focused checkpoint test; focused restore test passed, checking physical secret, projection counter, profiles and no retained handlers. Still no durable encoding, certified cache checkpoint, liveness policy or creator-process recovery. This must not be used to resurrect an expired/disbanded room.

Checkpoint duplicate-command regression: extended the authority restore test to reconnect on fresh transport and resubmit the exact pre-checkpoint Create envelope. Focused test passed: Applied replay response, unchanged revision, processed-command records and committed events. This proves reducer replay evidence survives the in-memory boundary. Source audit also identifies CertifiedDeviceRoom observation/action/route caches, service profiles, next service command and countdown anchor as additional restart state; these are not yet captured. Durable recovery remains incomplete.

Authority checkpoint foundation: `InProcessAuthority::checkpoint/from_checkpoint` preserve reducer state, committed events, manual clock, chat tail and delivery counter together, with fresh transport supplied on restore. Opaque checkpoint has private fields and no Debug/wire encoding because it can contain private hands. Runtime suite passed 36 tests, including a committed Create checkpoint/restore equality regression. This is in-memory scaffolding only: no durable serialization/encryption, certified replay/profile/action-source/physical-state checkpoint, persistence-before-ack, liveness/disband gate, or creator process recovery yet.

Creator recovery source audit (2026-09-09): `veilid_rendezvous.rs::resume_host_room` already reopens protected DHT ownership and rotates a route for the same application identity; it must not be mistaken for room-state recovery. `InProcessAuthority` owns state, manual clock, next delivery and committed events; `SharedLoopbackState` additionally owns profiles/routes, action source, projection counter, independent physical identity secret and poses. The published device service currently has no checkpoint restore path. Reconstructing with `InProcessAuthority::new(state, ...)` alone loses event provenance used for physical identity epochs. Existing transcript snapshot tests replay fixture prefixes, not production certified service recovery.

Next creator-recovery implementation sequence (all still required, not completed):

1. Define a versioned authority-only checkpoint that preserves reducer state, accepted command/replay evidence, service identity material, game-start/round provenance and physical identity/pose state. Distinguish restartable state from ephemeral route/capture registrations. Reject malformed/inconsistent checkpoints before publishing a resumed route.
2. Store sensitive checkpoint material only behind protected encryption; do not expose full private state to surviving player devices or diagnostics. Wire durable-before-ack ordering for state-changing commands/poses and prove fault cases around the write/ack boundary.
3. Resume the existing room route only after checkpoint validation and surviving-peer/liveness policy checks; never resurrect a room after final departure/all-peer expiry. Restore creator using the same protected identity and reconnect peer delivery to the rotated route.
4. Forced creator process loss with surviving peer must recover the same game/history/identities, followed by disband/stale-code negative tests. This does not promise that the remaining single player can finalize quorum actions while alone; do not lower quorum to make tests pass. Peer replication/failover acceptance remains part of the broader goal, not satisfied by local checkpointing alone.

Forced participant loss verified: production process probe now keeps the first joiner alive after motion confirmation, kills/reaps that exact child through its owned `Child` handle (requires non-success termination), then starts a fresh process using the same protected profile. Public Veilid test passed in 33.89 s, suffix 1788987097963; recovered seat, private hand and pose while creator stayed alive. Same elevated command as the restart evidence below. This adds abrupt participant loss evidence, not creator failover, arbitrary in-flight-write recovery or all-peer disband. No full T5 completion.

Verified participant process restart: updated public test passed 2026-09-09 in 47.64 s, protected profile suffix 1788986944617. Original joiner completed deal/motion and exited; a fresh process reused its protected profile and recovered seat 1, nonempty private hand and retained pose while creator stayed alive. Command: elevated `POCHE_ALLOW_VEILID_PUBLIC_TEST=I_ACCEPT_PUBLIC_NETWORK_TRAFFIC; cargo test --locked -p poche-cli --offline protected_desktop_two_process -- --ignored --nocapture`. Read retries occurred. Session 10384 ended successfully; prior 89613 ended failed after 150.09 s startup-related timeout, so earlier live-handle notes are historical. This is ordinary process exit/restart, NOT forced kill, creator crash/failover, all-peer disband or UI evidence. Those requirements remain pending; no T5 completion.

Recovery probe follow-up: CLI test compilation passed after the restart/fail-fast changes. Added redacted `VeilidRendezvousError` category logging at rendezvous initialization/publication so a future failure is actionable without exposing keys/routes. Previous public test session 89613 was re-polled and remains live waiting for its original joiner timeout; do not start a duplicate until it terminates. Recovery acceptance remains unverified.

Participant restart probe in progress: `process_probe.rs` now starts a fresh joiner process with the same protected profile after the original process exits, while creator remains alive; requires recovered seat/hand/pose. This is process restart, not forced kill or creator failover. First public-network run failed before reaching recovery (`Could not publish the lobby`, creator startup 12.78 s). Parent originally waited for joiner; amended source detects failed creator early. Existing running test session 89613 may still be finishing its bounded joiner timeout; inspect that handle before retrying. No recovery success claimed. Test change remains pending successful validation.

Transport credential checkpoint: `with_transport_password` provides an OS-vault-backed random password at a closure boundary, serialized by the creation lock. Callers must explicitly allow creation only for new transport storage; missing recovery credentials and malformed stored values fail closed. The password service is distinct from player/device signing services. New test proves stable lookup, distinct node credentials and no creation during missing-key recovery. Full protected_store run in sandbox passed six memory-backed tests but failed the Windows probe's initial read again. The same seven tests all passed with approved `require_escalated` execution outside the sandbox (0.10s), consistent with but not proof of a sandbox/logon issue. No secret values logged; the OS probe deletes its own uniquely named credential. Desktop startup still must determine new-versus-existing node storage before choosing allow_create and apply validate_protected_store_config before startup.

Protected startup checkpoint: `ProtectedProfileStore::load_or_create_device` recovers selected root/device profiles and verifies both protected keys; only missing public profiles are created, while corruption/missing secrets fail without identity replacement. Root/device creation now holds an OS file lock across vacancy check, vault write and public commit/rollback. Its production lock root is common to the OS user's vault even when public metadata roots differ; files are retained and locks release on process death. New tests exercise competing stores sharing a vault, retained winner credentials, stable recovery, distinct players and missing-key failure. `cargo test --locked -p poche-player-client --features protected-store --offline --lib protected_store` passed five tests before recovery test addition; after addition five memory-backed tests passed and the Windows vault probe failed on its initial load (line 934). Isolated rerun of `--lib windows_credential_vault_round_trip_deletes_its_probe` passed without a source fix. Do not call that intermittent failure resolved. The concurrency test uses separate store owners in threads with OS locks, not process-kill acceptance. Menu profile selection, active-device identity reuse policy and actual crash recovery remain pending.

Work: close G1/G2 fully. Persist credential references before admission succeeds; protect secrets, atomic profile writes, explicit recovery errors. Reconnect proves identity cryptographically, not by matching display name. New device enrollment must not clone a device identity or increase player rights/votes. Persist or reconstruct canonical accepted history and entitled private state according to agreed fault model. Distinguish disconnected, voluntarily left, kicked, and revoked.

Validation: `cargo test --locked -p poche-player-client --features protected-store`; `cargo test --locked -p poche-session -p poche-runtime`. Add process-kill tests after join/deal/action submission, replay without double application, wrong-secret/name-collision/expired-invite/revoked-device cases, secret-redacted logs/captures, and agreed creator/all-peer failure cases. Use temporary test profiles, never overwrite user keyring entries.

Complete when a killed/relaunched player resumes the same authorized identity/state within the agreed availability model; ordinary leave still permits spectator rejoin where allowed. Game state recovery must be proven independently of credential persistence.

U13 acceptance: final intentional departure disbands the lobby; reuse of its invitation reports closed/unavailable rather than recreating it. A new lobby uses a fresh identity/invitation. No all-peer offline restoration promise. Add unexpected-all-peer-loss tests under the chosen grace/expiry policy, temporary partition tests that cannot independently finalize conflicting histories, creator crash with a surviving peer, and multiple-device presence tests (one device closing is not automatically its player's final departure). Grace duration and protocol remain design decisions, not user-confirmed timing requirements.

### [ ] T6 Acceptance, docs, and handoff

Work: one documented desktop launch path and windowless puppet invocation; run menu-to-G4 endpoint plus T5 failures. CLI is another authorized device, not window IPC. Capture screenshots and semantic revision/history evidence, inspect images, redact credentials. Update README/coverage/release notes; preserve prior web artifacts with explicit support scope. Review diff, commit scoped work and follow repository push workflow when implementing the agreed goal.

Validation: `cargo test --workspace --locked`; `cargo run --locked -p poche-xtask -- coverage audit --all`; actual new end-to-end commands recorded here. Existing native test surface: `$env:WGPU_BACKEND='dx12'; cargo test --locked -p poche-puppet --test native_full_game -- --ignored --nocapture` (verify applicability before using it as new acceptance).

Complete when all tasks/gates have evidence, failures are resolved or honestly scoped, fresh captures inspected, docs reproduce two-window play and recovery, and final three-pass audit is recorded.

## Acceptance matrix and risks

| Surface | Required evidence | Current evidence |
| --- | --- | --- |
| Single-process mock nodes | Real adapter, fault injection, identities and privacy | Pending |
| Two Windows game processes | Real Veilid, UI menu/seats/agreed gameplay, crash recovery | Production connector create/join/seats/ready/deal passed in two processes; graphical input, motion and recovery pending |
| Windowless puppets | Same actions, no visible windows, actual GPU capture inspected | Pending |
| CLI sibling device | Same authorization and observations, no extra vote weight | Pending |
| Browser/other OS | Outside this slice; retain prior artifacts without claiming new validation | Not targeted |

Critical risks: credential recovery without surviving room state (G2/T5); stolen recovery capability (G1/T5); same-process mock falsely proving interprocess networking (T2/T6); speculative animation mistaken for accepted play (T4); all peers receiving private faces (T4); name collision granting identity (T5); feature unification accidentally shipping mocks (T2); scope drifting into general engine/UI rewrite (G4). Each must have a concrete negative test or documented gate disposition before release.
